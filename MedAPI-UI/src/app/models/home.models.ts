import { CheckEmptyUtil } from '../shared/util/check-empty.util';

export class Home {
  rooms = '';
  population = '';
  type = '';
  ownership = '';
  material = '';
  electricity = false;
  water = false;
  sewage = false;
  //constructor(data?: any) {
  //  console.log(data,'data.rooms')
  //  if (CheckEmptyUtil.isNotEmptyObject(data)) {
  //    console.log(data, 'data.rooms1')
  //    if (CheckEmptyUtil.isNotEmpty(data.rooms)) {
  //      this.rooms = data.rooms;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.population)) {
  //      this.population = data.population;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.type)) {
  //      this.type = data.type;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.ownership)) {
  //      this.ownership = data.ownership;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.material)) {
  //      this.material = data.material;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.electricity)) {
  //      this.electricity = data.electricity;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.water)) {
  //      this.water = data.water;
  //    }
  //    if (CheckEmptyUtil.isNotEmpty(data.sewage)) {
  //      this.sewage = data.sewage;
  //    }
  //  }
  // }
}
